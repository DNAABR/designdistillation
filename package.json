{
  "name": "design-distillation",
  "version": "0.5.0",
  "private": true,
  "description": "Machine-readable design knowledge, design profiles, and anti-slop rules for humans and AI coding agents.",
  "scripts": {
    "validate": "node scripts/validate.mjs",
    "test": "node --test scripts/token-validator.test.mjs scripts/corpus-validator.test.mjs scripts/recipe-validator.test.mjs scripts/recipe-blender.test.mjs scripts/composer.test.mjs scripts/composer-validator.test.mjs",
    "check": "npm run validate && npm test",
    "compose": "node scripts/compose.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "UNLICENSED"
}
